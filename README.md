# cloudwatch-codebuild-trigger

Creates a CloudWatch event to trigger CodeBuild build tasks.

## How To

1. Clone this repo
2. Run the standard terraform commands:

```bash
terraform init
terraform plan
terraform apply
```